## How to run the application:
1. Open the solution in Visual Studio
2. Once the application has loaded, right click on the solution name and select "Set Startup Projects"
3. On the window that will pop up (this should be Common Properties => Startup Project) select "RPNTest"
4. Press Ok and Click Start

## Requirements
The project mainly requires:
1. .NET Core Runtime version 3.1
